import { Box } from "@mui/material";

const About = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
      }}
    >
      Nothing to show here yet
    </Box>
  );
};

export default About;
