import { Box } from "@mui/material";

const Employees = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
      }}
    >
      Nothing to show here yet
    </Box>
  );
};

export default Employees;
