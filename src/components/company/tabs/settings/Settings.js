import { Box } from "@mui/material";

const Settings = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
      }}
    >
      Nothing to show here yet
    </Box>
  );
};

export default Settings;
